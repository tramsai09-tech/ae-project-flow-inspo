/**
 * ae-integration feature module
 *
 * Manages the connection between the browser app and the AE CEP panel.
 * Internal implementation:
 *   components/ — ExportPanel, ConnectionStatus
 *   hooks/      — useAEBridge
 *
 * Phase 4 implementation target.
 */

// Placeholder — Phase 4
export {};
