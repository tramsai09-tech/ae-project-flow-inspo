{
  "name": "@ae-motion-tools/bezier-core",
  "version": "0.0.1",
  "description": "Zero-dependency cubic Bézier math kernel for AE Motion Tools",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ae-motion-tools/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
