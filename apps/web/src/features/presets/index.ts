/**
 * presets feature module
 *
 * Exports the Presets feature entry point.
 * Internal implementation is split into:
 *   components/ — PresetPanel, PresetCard, PresetSearch
 *   store/      — presetStore (Zustand slice)
 *   data/       — builtinPresets (30+ curated curves)
 *
 * Phase 3 implementation target.
 */

// Placeholder — Phase 3
export {};
