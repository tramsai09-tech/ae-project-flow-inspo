/**
 * animation-preview feature module
 *
 * Exports the AnimationPreview feature entry point.
 * Internal implementation is split into:
 *   components/ — PreviewCanvas, PlaybackControls, TimelineRuler
 *   hooks/      — useAnimationLoop, usePlayback
 *   store/      — previewStore (Zustand slice)
 *
 * Phase 2 implementation target.
 */

// Placeholder — Phase 2
export {};
