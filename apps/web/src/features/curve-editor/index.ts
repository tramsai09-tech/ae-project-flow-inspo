/**
 * curve-editor feature module
 *
 * Exports the CurveEditor feature entry point.
 * Internal implementation is split into:
 *   components/ — React canvas and overlay components
 *   hooks/      — useCurveDrag, useCanvasRenderer, useHitTest
 *   store/      — curveStore (Zustand slice)
 *
 * Phase 1 implementation target.
 */

// Placeholder — Phase 1
export {};
